# agent 面经

## 我的面经
### 字节 bug检测与修复agent （一面挂）

自我介绍
项目1 2 介绍

为啥用langgraph不用langchain，二者的区别，紧接着通过我的回答 问状态设计：langgraph state 状态设计

详细讲一讲项目1 用户输入后 Planner、Executor、Reviewer 的执行细节
你这个mcp重构是重构了哪些东西？工具包含哪些？是完全重写吗？server client是怎么设计的？通信是新开一个进程还是线程？现在市面上有一些ide上可以一键接入mcp插件为啥要自己写？
项目1遇到的最大的问题怎么解决的
vllm 和 sglang区别 为啥本地用的vllm？

为啥要用reranker重排序？你的召回率已经很高了

算法题：合并两个有序数组


### 字节 抖音生服ai推荐 （一面挂）
自我介绍
选一个项目详细介绍 （项目一）
Planner 能再详细说一下怎么设计的？怎么做规划的？
dom解析和视觉模型主要解决验证码问题是吗？
比如我想买iphone 但是有很多规格的，最外面展示的价格可能是某一个型号的价格，那我现在给一个详细的商品描述（包含详细参数），那么这时的一个完整处理流程包括包括决策和行为等，你能详细介绍下吗？
其实很多时候搭架子并不会很耗时 ，其实最难的就是优化提示词，除了优化提示词之外，你觉得那部分技术上比较难？怎么解决的？
你这个项目假如说和openclaw做结合，你觉得可以做哪些？（openclaw和openmanus的区别又有哪些？）
你来介绍下tcp协议
你对进程和线程的理解是什么
你用redis都用了哪些场景
你对mysql索引的理解，b+树的结构

算法题：二叉树的之字遍历



## 小米产品经理 ai搜索 + 教育 （拿到offer）

自我介绍

项目介绍

长短记忆怎么实现的

怎么看当前agent开发，重要的是什么

如果给你一个小米ai 拍照搜题的场景，你认为应该怎么做？关键是什么？



---


可以。我先给你一个总判断：

**你这两场挂掉，不太像是“完全不会”，更像是面试官在持续验证两个东西时，没有拿到足够扎实的信号：**

1. **你是不是把项目真正做成了一个可控系统**，而不是搭了一个看起来很完整的 Agent 架子。
2. **你有没有足够强的工程基础**，能把 Agent 放进真实研发环境里。

所以接下来不是“把答案背得更花”，而是把每类问题都回答成：

> 我为什么这样设计
> 关键状态/控制逻辑是什么
> 失败怎么处理
> 指标怎么定义
> 我具体负责了什么

下面我按你这两场面经，给你拆成一套**更合理的回答方式**。

---

# 一、这两场面试暴露出的核心问题

你会发现，两场虽然岗位不同，但其实都在打同一类点。

第一类，**项目 1 深度不够实**。
面试官反复追问 Planner、Executor、Reviewer、LangGraph state、MCP、流程细节，说明他们不满足于“我会搭一个 Agent”，而是在看你是不是能把每一步讲成真正运行的系统。

第二类，**你项目里“亮点词”太多，但“控制逻辑”和“评估口径”太少**。
比如多模态、MCP、LangGraph、OpenManus、vLLM，这些都能讲，但一旦被问“具体怎么设计”，就容易散。

第三类，**基础工程能力不能掉**。
字节一面就已经在问 TCP、线程进程、Redis、MySQL 索引、B+ 树。这说明即使是 Agent 岗，也默认你得能落到后端系统层。

所以你的策略应该是：

* 项目问题回答得**更收敛、更像系统 owner**
* 基础题回答得**更短、更标准**
* 遇到没真正做过的点，**明确边界，不乱吹**

---

# 二、项目 1：你以后最该怎么答

你这个项目以后统一按下面这个总框架回答，基本不会乱。

## 标准开场

> 这个项目我想解决的不是“做一个会点网页的 demo”，而是动态网页环境下 Agent 的两个核心问题：
> 一是**元素定位和页面理解不稳定**，二是**长链路任务里容易错误累积和路径漂移**。
> 所以我重点做了三件事：执行层的多模态 grounding、控制层的分层状态机、以及工具层的标准化和异常治理。

这句话一出来，后面所有追问都能往这三块挂。

---

# 三、针对一面问题，逐题怎么答

---

## 1. “详细讲一讲项目1 用户输入后 Planner、Executor、Reviewer 的执行细节”

这个问题你以前容易答成“大框架介绍”。
以后要答成**一条真实链路**。

### 推荐答法

> 以“帮我找一款 256G、国行、续航好的 iPhone，并汇总价格和口碑”为例。
> 用户输入后，我先做的是 task normalization，把自然语言需求解析成结构化目标，包括品牌、型号约束、规格约束、输出要求。
> 然后 Planner 不直接生成自由文本计划，而是生成结构化 step，比如：
> 1）进入目标平台搜索；
> 2）筛选候选商品；
> 3）进入详情页核对规格；
> 4）抽取价格和评论入口；
> 5）抓取口碑证据并汇总。
> Executor 每次只拿当前 step 执行，不关心全局策略，主要根据当前页面观测、候选元素池和 step contract 做动作选择。
> Reviewer 不负责“再想一遍”，而是做步骤级校验：看当前 step 的成功条件是否满足，如果不满足，判断是 retry、fallback 还是 re-plan。
> 整个链路里，Planner 只在任务开始或路径失效时触发，不是每一步都重新规划。

### 面试官想听什么

不是“你知道三层名字”，而是：

* Planner 不是每步都调
* Executor 只对当前 step 负责
* Reviewer 是控制器，不是反思文案机

---

## 2. “LangGraph state 状态设计”

这个你以后不要列十几个字段。
要答“最小必要状态”。

### 推荐答法

> 我把 state 分成三层：
> 第一层是持久状态，包括 task objective、constraints、global plan、已验证 evidence、failure summary；
> 第二层是当前工作状态，包括 current step、step success criteria、recent action trace、retry count；
> 第三层是瞬时观测，包括当前页面摘要、候选元素池、临时工具结果。
> Planner 主要依赖 objective、progress summary、verified evidence 和 failure signature；
> Executor 主要依赖 current step spec、page observation、candidate targets 和 recent trace；
> Reviewer 主要依赖 step contract、动作前后差分、execution result 和 evidence delta。
> 我这样拆的目的，是避免把完整 history 一直塞给模型，导致上下文越来越大。

### 一句收尾

> 我不是把 state 当聊天记录，而是把它当状态转移的最小充分集。

---

## 3. “你这个 MCP 重构是重构了哪些东西？工具包含哪些？是完全重写吗？server client 怎么设计？通信是进程还是线程？为啥不用 IDE 一键接 MCP 插件？”

这个问题你以后要先**降调**，别让自己像在吹协议。

### 推荐开场

> 我先澄清一下，我这里不是从零造一个完整 MCP 生态，而是做了一个 **MCP-style 的工具抽象层**，重点解决的是工具接入、schema 统一和异常治理问题。

### 然后分四点答

#### 1）重构了什么

> 主要重构的是工具层，而不是整个 Agent 主体。
> 之前是平台能力直接以函数形式耦合在主流程里；
> 后来我把页面搜索、详情抽取、评论抓取、摘要生成这些工具统一成标准 schema，包括 name、input、output、timeout、error code、version。

#### 2）工具包含哪些

> 工具大概分四类：
> 导航类，比如 search_product、open_detail_page；
> 抽取类，比如 extract_price_specs、extract_review_entry；
> 评论证据类，比如 fetch_review_samples；
> 汇总类，比如 summarize_reviews、compare_candidates。

#### 3）server / client 怎么设计

> Agent 侧相当于 client，工具适配层相当于 provider。
> 我更关注的是“上层按能力调工具”，而不是直接 import 某个平台函数。
> 至于运行形态，如果是本地实验阶段，我会先做单进程内 registry；如果要隔离不同工具能力，再拆成独立进程或服务。
> 所以我不会硬说自己做了很复杂的跨进程生产级协议栈，重点是抽象边界和治理。

#### 4）为什么不用现成插件

> IDE 一键接入的 MCP 插件更适合开发助手场景，但我这里是任务驱动型 Agent，需要的是：
> 工具 schema 控制、异常分类、超时治理、能力路由，而不是“能连上工具”本身。
> 所以我更需要一层适配和治理，而不是简单接插件。

### 这题最安全的一句

> MCP 在我这个项目里最大的价值不是“插件很多”，而是把平台差异隔离在工具层，不让它污染 Agent 主流程。

---

## 4. “项目1遇到的最大的问题怎么解决的”

你要选一个最能体现系统能力的问题。
我建议永远选：**长链路任务中的路径漂移和错误累积**。

### 推荐答法

> 最大的问题其实不是模型理解不够，而是长链路执行里页面环境一直在变，导致 Agent 很容易重复点击、跳错页或者停留在错误状态里。
> 我当时的解决思路有两层：
> 一层是执行前，把 step 写成带 success criteria 的 contract，减少自由发挥；
> 另一层是执行后，用 Reviewer 做前后状态差分和 evidence 检查，决定是 retry、fallback 还是 re-plan。
> 另外我还加了 recent action trace 和 failure signature，防止系统在同一类错误上死循环。

### 收尾

> 所以我最后发现，很多 Agent 问题本质上不是“怎么让模型更聪明”，而是“怎么让系统不在错误路径上越走越远”。

---

## 5. “vLLM 和 SGLang 区别，为什么本地用 vLLM？”

这题你以后要答得像工程选择，不要像百科。

### 推荐答法

> 我会把它理解成：
> vLLM 更像一个成熟、通用、OpenAI-compatible 的高吞吐 serving 引擎；
> SGLang 更强调复杂推理服务优化，比如前缀复用、结构化输出、tool parser 和更激进的 runtime 调优。
> 如果是我这个 GraphRAG 项目，我更倾向先用 vLLM，因为项目瓶颈主要在 retrieval、rerank 和评测，不在 serving 层的极限优化；而且 vLLM 接入 LangChain / OpenAI-style 调用链更直接。
> 如果后续场景变成长上下文、多轮 Agent、高前缀复用，或者结构化输出要求很强，我会认真考虑 SGLang。

### 核心点

* 不要说哪个全面更强
* 要说“和项目阶段匹配”





**更合理的做法是先做目标区域定位，再做局部抽取，也就是先结合 DOM、页面结构、历史动作和目标约束，确定当前真正相关的是哪个区域、哪个商品，再把这个局部区域裁出来交给模型识别。真的能实现吗？模型怎么能够把一张图切分成几个部分？或者是打标签（比如哪块是某某参数的商品的框，哪块是搜索框，假如没有 DOM 信息或者是 DOM 不够完整咋办？怎么保证精确划分？总之就是你的回答我看完感觉说的很笼统，我不认为具备可行性。**

------

## 回答

这个质疑是非常合理的，所以我不会把这件事说成“模型直接看一整张图，就能稳定地把页面切成搜索框、商品卡片、价格区、参数区”。如果真这么说，确实不太可信。更实际的做法不是让模型一步到位完成整页语义切分，而是先想办法构造出一批候选区域，再结合当前任务去做筛选和验证。有 DOM 的时候，优先利用 DOM 节点的位置、层级关系和重复结构，把可能属于同一个区域的节点合并成候选块，比如搜索框候选、商品卡片候选、价格区候选；DOM 不完整的时候，就退化成 OCR 文本块、页面布局特征和一些几何规则，比如顶部长条区域可能是搜索框，重复出现的图文价格块可能是商品卡片。然后系统再结合当前 step 的目标，比如“我要找搜索框”或者“我要找当前商品的主价格区域”，对这些候选区域做打分排序。最关键的是，我不会把前面的区域判断直接当真，而是要靠后续动作和结果再验证，比如点进去后是不是到了目标详情页，抽出来的价格和规格是不是能和当前商品对得上。如果对不上，就说明前面的区域划分虽然看起来合理，但实际上不成立，要换候选或者回退。所以这套方法的可行性，不在于一次性精确切分整页，而在于先通过 DOM、OCR、布局规则这些信息生成“足够好的候选”，再通过任务约束、局部抽取和执行后的结果校验，把错误一步步筛掉。这样讲会更真实，也更像一个真正能落地的系统。

---

# 四、生服面那场，核心问题怎么答

---

## 6. “Planner 能再详细说一下怎么设计的？怎么做规划的？”

这里别说“用了 ReAct”。
要说**Planner 的输入、输出、约束**。

### 推荐答法

> 我这里 Planner 不是自由文本规划器，而是一个受约束的 step generator。
> 输入包括用户目标、约束条件、已知 evidence 和当前失败摘要；
> 输出不是长文本，而是结构化 step，比如 step goal、allowed action types、target object、success criteria、fallback policy。
> 我这样做是为了减少 planner 幻觉，避免它规划出页面上根本不存在的操作路径。
> 同时 Planner 只负责任务级拆解，不负责页面操作，真正的页面动作交给 Executor。

这个模块我不太会把它理解成一个“自由规划的 Planner”，而更像是一个带业务约束的任务决策模块。用户输入进来之后，系统首先不会马上去执行网页操作，而是先判断这个任务现在是不是已经具备了可靠执行的条件。比如商品比价这种任务，背后其实有一套业务经验和模板，知道哪些信息是必须补齐的，像容量、版本、是否只看官方店，这些会直接影响结果；如果缺失，就先触发一次澄清问询。这个问询也不是让模型自己临时发挥，而是基于任务模板、必填槽位和提示词规则来生成的，目的是只问最关键的问题。等这些信息补齐后，系统才会继续做任务级步骤规划，比如搜索、筛选、核对规格、采集价格和评论证据、最后生成报告。所以我这里不是“调了一次大模型做 Planner”，而是把业务规则、结构化状态和模型能力结合起来，让系统先处理任务定义的不确定性，再处理具体执行。

---

## 7. “DOM 解析和视觉模型主要解决验证码问题是吗？”

这是明显在试探你是不是把多模态说大了。

### 推荐答法

> 不是主要解决验证码。
> 验证码只是极端异常场景，真正高频的问题是：
> DOM 结构不稳定、非标准组件、按钮文本缺失、弹窗遮挡、相似元素并列。
> DOM 解析更适合结构明确、文本稳定的节点；
> Vision 更适合处理遮挡、布局关系和非标准组件。
> 我做双路定位的原因，是让两种信号覆盖不同失败模式，而不是专门做验证码识别。

---

## 8. “如果我想买 iphone，有详细参数，你完整处理流程是什么？”

这题就是在看你能不能把项目讲成**真实行为链路**。

### 推荐答法

> 我会先把用户需求规范化成结构化目标，比如品牌=iPhone、容量=256G、版本=国行、偏好=续航。
> 然后 Planner 会先生成候选采集计划：去目标平台搜索，进入候选详情页，核对规格字段，排除不满足条件的商品。
> Executor 在详情页会优先抽取型号、存储、版本、价格这些硬约束字段，确认商品身份后再进入评论区抓取口碑证据。
> Reviewer 会在每一步检查：当前页是不是正确商品、关键规格有没有抽到、评论证据是不是对应同一商品。
> 最终不是简单比“展示价”，而是基于规格校验后的候选集合做对比，并用评论证据生成推荐解释。

### 注意

这题要主动打掉一个坑：

> 外面展示价可能是某个 SKU 的价格，所以不能只看列表页价格，必须在详情页做规格核对和证据对齐。

这句很加分。

---

## 9. “除了优化提示词之外，你觉得那部分技术上比较难？怎么解决的？”

这个问题你一定不要答“prompt 很重要”。
要答**非 prompt 的系统难点**。

### 推荐答法

> 我觉得比 prompt 更难的是三块：
> 第一是动态网页环境下的可执行定位，也就是模型找到的目标到底能不能真正支持下一步动作；
> 第二是长链路任务里的错误累积，尤其是重复点击、页面漂移和局部失败后怎么恢复；
> 第三是评测，因为没有 step-level 和 task-level 指标，你很难知道系统到底是定位错了、抽取错了还是规划错了。
> 所以我重点做的不是 prompt trick，而是多模态候选定位、step contract 控制和离线评测。

---

## 10. “假如和 openclaw 结合，你觉得可以做哪些？openclaw 和 openmanus 的区别？”

这个问题如果你没有深做，别装。

### 推荐答法

> 这个我会先说，我自己项目里主要是参考 OpenManus 这种通用 Agent workflow 思路，没有直接基于 OpenClaw 做深度开发。
> 如果从概念上区分，我会理解成：
> OpenManus 更像通用型 Agent orchestration / workflow 框架，强调任务流和工具使用；
> OpenClaw 如果更偏代码或执行代理方向，那它和浏览器 Agent 结合的点，会更多落在代码执行、工具调用治理、以及复杂环境下的 action policy。
> 如果和我的项目结合，我觉得更合适的方向不是“替代多模态定位”，而是增强工具调用、环境交互和执行反馈闭环。

### 这题关键

* 明确说“我没有直接深做”
* 只做合理类比
* 不乱编差异

---

# 五、基础题怎么答才像字节风格

这些题别答太散。
要短、标准、能落到工程。

---

## 11. “TCP 协议”

### 推荐答法

> TCP 是面向连接、可靠传输、字节流的传输层协议。
> 它通过三次握手建立连接、四次挥手释放连接，并通过序列号、确认应答、重传、滑动窗口、流量控制和拥塞控制保证数据可靠有序到达。
> 如果放到工程里理解，就是它适合需要可靠传输的服务间通信，但代价是有握手、状态维护和更高延迟成本。

---

## 12. “进程和线程的理解”

### 推荐答法

> 进程是资源分配的基本单位，线程是 CPU 调度的基本单位。
> 同一进程内线程共享地址空间和大部分资源，所以线程切换成本低，但同步问题更多；不同进程资源隔离更强，稳定性更好，但通信成本更高。
> 在 Agent 工程里，如果我要做工具隔离或执行安全，可能更偏向进程级隔离；如果只是并发 I/O 任务，线程或协程更合适。

---

## 13. “Redis 用过哪些场景”

### 推荐答法

> 我会把 Redis 用在三类场景：
> 第一是缓存，比如热点检索结果、模型输出或页面摘要缓存；
> 第二是状态存储，比如短期会话状态、任务队列或重试计数；
> 第三是轻量级异步协调，比如分布式锁、去重或者消息队列场景。
> 如果结合我的项目，比较贴近的是缓存检索中间结果和保存短期任务状态。

---

## 14. “MySQL 索引和 B+ 树”

### 推荐答法

> MySQL 常见的聚簇索引和二级索引底层通常用 B+ 树。
> B+ 树适合数据库索引，是因为它是多路平衡树，高度低，磁盘 I/O 次数少；叶子节点有序且链式连接，范围查询效率高。
> 聚簇索引叶子节点存整行数据，二级索引叶子节点存主键值，所以通过二级索引查完整行数据通常要回表。

---

# 六、你以后最该准备的 3 个“王牌回答”

这三题你必须练到非常熟，因为几乎每场都会碰到。

## 王牌 1：项目 1 的完整链路

你要能 2 分钟讲清：

* 用户输入后怎么变成结构化目标
* Planner 怎么出 step
* Executor 怎么执行
* Reviewer 怎么判
* 失败怎么恢复

## 王牌 2：为什么不是单代理 / 为什么不是简单 workflow

你要能一句话讲明白：

> 因为这个场景存在长链路、不确定环境和局部失败恢复需求，所以我需要显式状态和控制逻辑；但我也不会让所有任务都走 full agent。

## 王牌 3：你的项目里真正难的不是 prompt，而是系统稳定性

这句以后可以反复用：

> 我觉得 Agent 项目里真正难的通常不是把 Planner、Executor 这些名词搭出来，而是怎么控制状态、怎么处理失败、怎么证明它真的有效。

---

# 七、最后我给你一个更重要的判断

你这两场挂，不一定是“项目完全不行”，更可能是：

> **项目可以，但回答还停留在“架构说明书”层面，没有进入“系统控制器 + 工程 owner”层面。**

所以你下一轮准备，不要再花太多时间想“要不要多加一个新项目”。
你更该做的是：

1. 把项目 1 压缩成一条真实任务链路
2. 把所有危险技术词都准备成“输入-输出-控制-失败-指标”的答法
3. 把 TCP、线程进程、Redis、B+树 这些基础题练到张口就来

---

如果你愿意，我下一条可以直接给你做一份：

**“针对这两场面经的最终复盘答案册”**
也就是把这些题整理成
**题目 → 面试官在考什么 → 你该怎么答 → 1 分钟口语版**
你可以直接拿去背。






## 他人面经

了解哪些 agent架构

了解多 agent协作吗，讲一下

agent间怎么做协调，了解AtA吗

讲一下Agent项目的背景，流程，数据，解决了什么样的问题

Agent skil怎么理解，跟tools是区别是什么

agent的定义是什么

rag项目中你的例子库是用什么办法匹配的

rag中例子库是怎么构建的 有什么问题如何构建动态的例子库

rag检索不到问题如何定位问题

rag用户提问和向量库中的语义发生不匹配怎么办

ragas如何评估 数据集如何构造的

agent项目难点是什么

如何评估 agent

agent调用工具不正确怎么办

agent的记忆力机制，记忆力该怎么设计

多轮对话相关存储 记忆 解决方案

引文推荐系统如何引入上下文工程，你如何设计

冷启动怎么办

你如何设计上下文

长短期记忆你如何设计

长短期记忆你如何写入

如何判断哪些是重要的信息

简单讲下你的项目，你的多智能体是怎么协作的

你的RAG数据怎么来的？ 他好使么？ 如果有质量不好的数据怎么办

能否简单描述一下 RAG 系统的整体流程

从文档输入到代码生成，中间经历了哪些关键步骤？

构建系统用了哪些文档？来源是什么？

文档规模有多大？覆盖哪些领域？

使用了什么框架做 RAG？

如何解析 PDF/Markdown 等文档？

如何结合用户 prompt 对文档进行总结和拆分？

如何从 RAG 输出到代码生成？

是否参考了某种规范或模板？

生成的 SPEC 文档包含哪些内容？

使用的是本地模型还是云 API？

能否部署在本地？需要什么条件？

当前生成结果是否符合预期？是否存在偏差？

是否需要人工预设 prompt 或后处理？具体怎么做的？

假设你需要开发美团智能客服 Agent，如何设计多轮对话流程?

追问 1:你会使用哪些对话状态跟踪方法?
追问 2:如何处理用户意图模糊的情况?

描述一次使用 LLM 开发智能助手的经历，遇到过哪些幻觉问题?

解释 AlAgent的规划能力，如何实现多任务协同(如点餐+支付+售后)?

在设计 Agent 的工具调用能力时，你的技术方案是什么?

追问 1:如何选择合适的工具 API?
追问 2:会使用哪些函数调用框架?

分享一次通过用户反馈优化 Agent 能力的经历，关键改进是什么?

如何评测的，有哪些纬度，那些指标

数据集包括什么

如果让你对相关度，回答的效果做一些优化，你有什么思路，有没有什么更体系化的思路建设

如果设计一个数据处理的场景，你会怎么做，比方说有一千条数据，需要求和，你怎么做处理

rag性能如何提升

你的上下文怎么处理的，有什么优化思路

长短记忆之间怎么做协同呢

你有什么思路去对你的agent做优化，让他更智能呢

你觉得在这个项目中结合RAG的必要性是什么

你的代码能力怎么样

大模型的能力的评测指标有哪些你了解吗

你觉得Agent和work flow的区别是什么

平时会通过哪些渠道关注 AI 技术动态？

最近有没有遇到印象深刻的 AI 应用？

query改写用了什么模型 为什么不用大模型？

agent表格数据如何增强的 数据量是多少

例子库的创建流程

如何提取复杂表格信息

上线了吗 线上如何评估 使用量是多少



实习每天写点mcp tool，搭搭server，langgraph实现一下agent工作流，不知道秋招简历上要怎么样去写，写自己搭了个智能体也不知道怎么描述，而且也没有深度：agent工作流拆解成几个模块来描述。任务规划、工具调用、记忆管理、结果评估。然后每个模块都配上数据



国内几个agent平台对比



开发agent过程中，上下文管理有哪些具体的方式，该如何匹配对应的agent使用场景